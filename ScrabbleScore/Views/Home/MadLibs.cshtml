<!DOCTYPE html>
<html>
  <head>
    <title>Hello Friend!</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <div class="container">
      <p>One day, @Model.Person1 and @Model.Person2 were walking through the woods, when suddenly a giant @Model.Animal appeared. "@Model.Exclamation, @Model.Person1 cried. The two of them @Model.Adverb @Model.Verb as quickly possible, and when they were safe, @Model.Person1 and @Model.Person2 gave each other a giant @Model.Adjective @Model.Noun.</p>
    </div>
  </body>
</html>